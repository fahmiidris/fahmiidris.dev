import banner from "./images/banner.jpg";

export const meta = {
    title: "Difference between Internal and External Link",
    description: "Custom link components that can distinguish internal and external links.",
    published: 1655744400000,
    image: banner.src,
};

This component checks the href property whether it starts with `https://` or `/`, if it starts with `https://` it will be considered an external link and if it only starts with `/`, then the link is included in the category of internal links.
